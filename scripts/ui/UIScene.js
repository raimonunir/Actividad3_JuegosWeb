class UIScene extends Phaser.Scene {
    constructor() {
        super('UIScene');
    }

    create() {
        // Crear elementos de HUD como puntuación, vidas, etc.
    }

    updateScore(value) {
        // Actualizar HUD
    }
}